display:
  default:
    display_title: 'Par défaut'
    display_options:
      fields:
        changed:
          settings:
            future_format: '@interval hence'
            past_format: '@interval ago'
